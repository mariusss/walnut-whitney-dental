[build]
  command   = "npm install --legacy-peer-deps && npx vite build --config vite.netlify.config.ts"
  publish   = "dist/public"

[build.environment]
  NODE_VERSION = "22"

[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
